<div class="page-header">
	<h1>All Rides</h1>

<div class="btn btn-primary">
<%= link_to 'Add New Ride', new_recent_path %>
</div>
<!--
<% @recents.each do |recent| %>
<div class="recents">
	<div class="date">Date: <%= recent.Date %> </div>
	<div class="distance">Distance:<%=link_to recent.distance, recent%></div>
    <div class="meta">
		<span><%= recent.Date %></span>
        <span><%= recent.route %><span>
        <%= recent.time %>
        <%= recent.distance %>
        <%= recent.average %>
        <%= recent.fastest %>
        <%= recent.notes %>
	
	</div>
	<span class="admin">
		<%= link_to "Edit", edit_recent_path(recent) %> |
		<%= link_to "Delete", recent, method: :delete, data: {confirm: "Are you sure you want to delete this entry?"}%>
	</span>
</div>

<% end %>
-->



<table class="table">
  <thead>
    <tr>
      
      <th>Date</th>
      <th>Route</th>
      <th>Time</th>
      <th>Distance</th>
      <th>Av.Speed</th>
      <th>Fastest</th>
      <th>Notes</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @recents.each do |recent| %>
      <tr>
        
        <td><%= recent.Date %></td>
        <td><%= recent.route %></td>
        <td><%= recent.time %></td>
        <td><%= recent.distance %></td>
        <td><%= recent.average %></td>
        <td><%= recent.fastest %></td>
        <td><%= recent.notes %></td>
        <td><%= link_to 'Show', recent %></td>
        <td><%= link_to 'Edit', edit_recent_path(recent) %></td>
        <td><%= link_to 'Destroy', recent, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>


